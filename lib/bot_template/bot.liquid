name     "{{ name }}"
version  "{{ version }}"
summary  "{{ summary }}"
author   "{{ author }}"
license  "{{ license }}"
helptext "{{ helptext }}"

implement do
  puts "Hello, world!"
end

{% if triggers.size > 0 %}
  {% for trigger in triggers %}
trigger "{{ trigger }}" do
  puts "Added a trigger: {{ trigger }}"
end
  {% endfor %}
{% endif %}
